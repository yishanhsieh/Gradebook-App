import { StyleSheet, Text, View } from "react-native";

export default function StudentGradeInfo() {
  return (
    <View>
      <Text>{stuGrade}</Text>
      <Text>{stuAbsence}</Text>
    </View>
  );
}
